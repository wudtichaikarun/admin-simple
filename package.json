{
  "name": "4pl-web-poc",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.23.5",
    "axios": "^0.19.0",
    "babel-plugin-import": "^1.12.2",
    "customize-cra": "^0.8.0",
    "flow-typed": "^2.6.1",
    "jest": "^24.9.0",
    "jest-cli": "20.0.4",
    "jwt-decode": "^2.2.0",
    "ramda": "^0.26.1",
    "react": "^16.10.1",
    "react-app-rewired": "^2.1.3",
    "react-dom": "^16.10.1",
    "react-loadable": "^5.5.0",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.1.2",
    "react-spring": "^8.0.27",
    "styled-components": "^4.4.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "$(npm bin)/flow && react-app-rewired test --env=jsdom",
    "eject": "react-scripts eject",
    "flow": "$(npm bin)/flow",
    "clean": "rm -rf build",
    "postinstall": "$(npm bin)/flow-typed install"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "flow-bin": "^0.108.0"
  }
}
